<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <style>
            body {
                background: linear-gradient(109.6deg, rgb(36, 45, 57) 11.2%, rgb(16, 37, 60) 51.2%, rgb(0, 0, 0) 98.6%);
                color: rgb(245, 240, 227);
                font-family: 'Aovel Sans', sans-serif;
            }

            .wildtitre {
                font-family: 'Geometri Outine', sans-serif;
                font-size: 60px;
                text-align: center;
            }

            img {
                height: 270px;
                width: auto;
                object-fit: cover;
                border-radius:3%;
                display: flex;
                justify-content: center;
            }

            h1 {
                text-align: center;
                font-size : 25px;
            }

            p {
                text-align: center;
                font-size : 20px;
            }
        </style>
        <link href="http://fonts.cdnfonts.com/css/geometri-outine" rel="stylesheet">
        <link href="http://fonts.cdnfonts.com/css/aovel-sans-2" rel="stylesheet">
    </head>


    <body>
    {% block header %}
    <nav>
        <div class="wildtitre">Wild series</div>
        <ul>
            <SELECT name="catégories" size="1">
            <option>Catégories</option>
            {# {% for category in categories %} #}
            {# <option>{{ category.name }}</option></a> #}
            {# {% endfor %} #}
            </SELECT>

            <SELECT name="acteurs" size="1">
            <option>Acteurs</option>
            </SELECT>

            <li><a href="">Mes favoris ❤</a></li>

            <li><a href="">Déconnexion</a></li>
        </ul>
    </nav>
    {% endblock %}

    {% block body %}
    {% endblock %}
    </body>
</html>
